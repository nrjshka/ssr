import React from 'react';

const Home = () => (
  <div>
    <div>
      Test
    </div>
    <button onClick={() => console.log('Hi, there!')}>
      Click me!
    </button>
  </div>
);

export default Home;